@import 'config.pcss';

.link {
  display: block;
}

.part {
  display: inline-block;
}

.url {
  color: var(--color-gray--52);

  max-width: 100%;

  white-space: nowrap;
  overflow-x: hidden;
  text-overflow: ellipsis;
}

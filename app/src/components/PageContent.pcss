@import '../config.pcss';

.pageContent {
  width: calc(100% - 32px);
  margin: 0 auto;

  @media (--medium-viewport) {
	width: calc(100% - 80px);
  }

  @media (--large-viewport) {
	width: 100%;
	max-width: 600px;
  }
}
